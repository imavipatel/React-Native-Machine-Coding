import { StyleSheet } from 'react-native';

export const RestaurantListFilterApiIntegrationStyle = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
