import { StyleSheet } from 'react-native';

export const NavBarStyles = StyleSheet.create({
  container: {
    height: '7%',
    paddingHorizontal: 16,
    alignItems: 'center',
    flexDirection: 'row',
  },
  titleStyle: {
    marginStart: '10%',
  },
});
